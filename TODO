- correct meta tags for landing page
- improve error message if q.include() fails in template
- removing a directory makes gulp terminate with an error
- watch _includes for changes
- how to access hash keys with special characters?
- structure images, make new dir /images/landing-page
- errors in q.include are not reported
- test writeAsset() from templates
- gettext API, can we use l_(...), l_x(...) as function names?
- xgettext_tt extractor
- sorting of assets or more generally of arrays with TT
- when moving an asset into the parent directory, we cat a "directory not
  empty" error
- explain how markdown treats block elements special, and what that means for
  using TT2 code inside or from markdown files
